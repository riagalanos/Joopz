<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.cscheerleader.joopz">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_joopz"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_joopz"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".DashboardActivity">
        </activity>
        <activity android:name=".ChallengeActivity">
        </activity>
        <activity android:name=".ProblemChooserActivity">
        </activity>
        <activity android:name=".AddChallengeActivity">
        </activity>
    </application>

</manifest>